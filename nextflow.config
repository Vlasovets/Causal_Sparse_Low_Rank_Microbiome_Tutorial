process {
    executor = 'local'  // Use 'slurm' when run on HPC
    withDocker = true
    container = 'rpy2:latest'
    cpus = 2
    memory = '4 GB'
}

docker {
    enabled = true
    runOptions = '-v ~/Causal_Sparse_Low_Rank_Microbiome_Tutorial:/container/mount/point/'
}
